import { PaginationModel } from "@/types/pagination";

let pageDefault: PaginationModel = {
    totalRecords: 10,
    totalPages: 1,
    currentPage: 1,
    pageSize: 10
}

export default pageDefault;